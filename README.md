# OOP_Lab_1
Class hierarchy.

The hierarchy of the restaurant has been created.
* The class hierarchy has 3 levels of inheritance.
* 6 different classes, which entities can be created.
* All classes have attributes (fields or properties).
* Used a static constructor.
* Used virtual, regular and static methods.
* All classes are divided into modules.
